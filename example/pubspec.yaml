name: example
description: Example app for flutter_telnyx_voice_ai_widget
version: 1.0.0+1

environment:
  sdk: ^3.5.0
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_telnyx_voice_ai_widget:
    path: ../
  cupertino_icons: ^1.0.8
  permission_handler: ^11.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

dependency_overrides:
  flutter_webrtc: ^0.14.2

flutter:
  uses-material-design: true